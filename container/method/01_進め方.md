# 進め方

## 大原則（どの仕事でも変わらない）

ここに書いてあるのは、仕事の目的が何であっても守るものだけです。
最初の仕事は基礎調査（`04_基礎調査の型.md`）で、そのあとはユーザーと対話しながら進めます（`02_基礎調査のあと.md`）。
基礎調査で書くもの・書かないものは `04` に、別の種類の仕事（監査など）を頼まれたときの進め方は `02` にあります。

### 1. ユーザーの求めに応え、アカウントの中は聞く前に調べる

- ユーザーの質問には、その場で答える。依頼は、その場でやる。計画や承認を待たせない。
- AWS の API で分かることは、ユーザーに聞く前に自分で調べる。聞くのは AWS の外のこと（利用者・運用・他のサービス）。
- 頼まれていない評価や提案を、気を利かせて書かない（基礎調査は事実の把握。`04`）。

### 2. 事実と推測を必ず分ける

- 事実 = API のレスポンスに実際に含まれていたもの
- 推測 = そこから導いた解釈

書き分けの例:

ECS クラスタ `app-prod` にサービスが 2 つある（事実）。
名前から API と Web に対応すると思われるが、コンテナイメージを確認していないため未確認。

推測を事実のように書かないでください。 後から読む人が検証できなくなります。

### 3. 読めなかったものも記録する

権限で拒否されたものは「拒否された」と書いてください。空白と「確認できなかった」は別物です。
セッションポリシーで意図的に塞いでいるものもあるため、拒否自体は異常ではありません。

### 4. 再現できるようにする

叩いたコマンドは、通ったものも拒否されたものも `out/.survey/env/aws-audit.log` に 1 行ずつ自動で残ります。
自分で列挙し直す必要はありません。残すべきなのは**判断**です。何を見てそう書いたか、何を訂正したかを、その回の調査ログに書いてください。
統合版には書きません。あちらは構成を語る文書で、作業の記録ではありません。

### 5. 断定を避ける場面

複数の世代が並行稼働していることがあります（`report/前提.md` を確認し、
書かれていなければユーザーに聞いてください）。
そのとき、「重複している」「使われていない」と見えるものが現役の可能性があります。
断定せず、判断材料（最終更新日時、メトリクス、アクセスログの有無）を添えてください。

これは評価してはいけないという意味ではありません（基礎調査では評価を書かず、監査を頼まれたら評価が成果物です。`04`・`02`）。
評価するにせよしないにせよ、根拠を添えるということです。

---

## セッションの型

この作業環境は頻繁に作り直され、資格情報にも有効期限があります。
`out/` だけが唯一の記憶です。次の型を守ってください。

### 開始時

`out/` に台帳や報告がまだ無いなら、基礎調査からです（`method/00_開始と再開.md`）。**とくに `out/.survey/env/check.md` が無ければ、
`03_環境の確認.md` を先にやってください。**

1. `survey-status` を実行する（残り時間と、いま何をすべきか、使える経路が表示されます）
2. `out/.survey/state.md`（台帳）で、いま何の作業をしているか・現在地・次にやること・決定事項を確認する
3. `out/report/` で、これまでに人に渡したものを確認する。`ユーザー確認事項.md` の回答欄にユーザーが書いていれば、それが最初の入力です（下記）
4. `out/report/前提.md` で、対象について分かっていることを確認する
5. `aws sts get-caller-identity` で身元を確認する
6. ユーザーの指示があればそれを、無ければ「次にやること」から着手する

その回の調査ログは `out/.survey/log/` に置きます（下記「成果物の置き場所」）。

その回にどこまでやるかは、`survey-status` の表示に従って決めてください。
ただしユーザーが応答できない回は、一時キーが自動で入れ替わるので、この表に従わず報告まで書き切ります（`00`）。

| 表示 | 判断 |
| --- | --- |
| 新しい領域に着手して構いません | 新しい領域に入ってよい |
| 着手中の領域を仕上げてください | いま触っている範囲を閉じる。新しい領域には入らない |
| 新しい作業に着手しないでください | 着手しない。 保存と台帳の更新に充てる |

1 回のセッションに与えられる時間は、回によって違います。
判断の境目は `survey-status` がその都度計算して表示します。
「残り何分なら何をしてよい」を自分で決め打ちにしないでください。
表示は作業中いつでも取り直せます。長い作業に入る前に見直してください。

### 終了時（必ずやること）

1. その回の調査ログを `out/.survey/log/` に保存する
2. **分かったことを `report/構成報告.md` の本文に反映する。** 統合版がまだ無いなら書く（基礎調査は報告まで書いて 1 回。`04`）。
   既にあるなら、**該当箇所を直接書き直してください**（末尾に追記しない・「追加調査で判明」と注記しない。
   下記「統合版に履歴を書かない」）
3. 人が調べないと答えが出ないことを `report/ユーザー確認事項.md` に移す（下記）
4. 台帳（`out/.survey/state.md`）を更新する（下記「台帳」）
5. その回に「次からはこう調べればいい」と分かったことを `out/.survey/notes.md` に書く（下記）

統合版に一通り書き終えた回は、そこで終わりにしてください。
**自分が書いた統合版を同じ回で検証しない**（`method/05_報告の検証.md`）。
台帳の「次にやること」に検証を書いて渡します。

### `ExpiredToken` が出たら

作業を止め、その時点までの結果を `out/` に保存し、進捗を更新してから、
ユーザーに再発行を依頼してください。書きかけのまま失わないことを優先します。

---

## 成果物の置き場所

`out/` は 2 つに分かれます。**人が読むのは `report/` だけ**で、`.survey/` は生データとこの環境の記録です。
平置きにしないでください。

```
out/
├── report/                  人が読む。調査の成果物
│   ├── 構成報告.md          統合版。いま何がどうなっているか
│   ├── ユーザー確認事項.md  ユーザーに答えてもらう問い。回答欄がある
│   └── 前提.md              対象について、ユーザーから聞いて分かったこと。あなたが書き、育てる
└── .survey/                 この環境の記録。人は読まない
    ├── raw/                 API の生データ（`raw-` で始める）。仕事を跨いで共有し、引き直さない
    ├── state.md             台帳。いまの仕事・現在地・次にやること・決定事項
    ├── notes.md             次も効く調べ方と読み違いの類型
    ├── log/                 各回の調査ログ。判断と訂正の経緯
    └── env/                 動作確認（`check.md`）と監査ログ（`aws-audit.log`。書けません）
```

`.survey/` はドットで始まるので `ls out/` には出ません。`ls -A out/` か、上のパスを直接読んでください。

### 一次記録が 2 つ、成果物が 1 束

| | 何か | 何が書いてあるか | いつ読むか |
| --- | --- | --- | --- |
| `.survey/raw/` | API の生データ | 機械が吐いたそのまま | 問いが起きたとき `jq` で引く |
| `.survey/log/` | その回の調査ログ | 何を見て、何を判断し、何を訂正したか | 統合版を書くときだけ |
| `report/` | 成果物 | いま何がどうなっているか | いつも |

基礎調査で `report/` に置くのは `構成報告.md`（統合版）と `ユーザー確認事項.md` の 2 つです（`04`）。`前提.md` はユーザーから聞いたことを
積む場所で、初回は見出しだけです。別の種類の仕事を頼まれたら、頼まれたもの（監査の指摘一覧・構成図・計画書など）を
`report/<仕事の名前>.md` として足します。**ファイル名に番号は付けません。**

`.survey/log/` は使い捨てです。統合版に取り込んだら、以後は基本的に読み返しません。
同じ問いが起きたら `log/` を遡るのではなく **`raw/` を引き直してください**。
`log/` を読み直すのは `raw/` を読み直すのと結局同じことで、しかも解釈が一枚挟まっている分だけ不正確です。

`log/` の中は `01-baseline.md` のように、2 桁の通し番号と英語の短い名前（`NN-<topic>.md`。小文字と `-` だけ）で、調べた順に増やします。
`.survey/` は人が読まないので、ファイル名は英語です。1 回のセッション、または 1 領域につき 1 ファイルです。
書くのは、冒頭に調査日時、本文に判断と訂正の経緯（前回の記述を訂正したなら、訂正したと書く）です。
**叩いたコマンドの列挙は要りません。** 通ったものも拒否されたものも `.survey/env/aws-audit.log` に 1 行ずつ残っています。

### 統合版に履歴を書かない

`report/構成報告.md` は**現在形**で書きます。いま何がどうなっているか だけを書いてください。

| ✗ 書かない | ✓ こう書く |
| --- | --- |
| 「第 3 回では〜としたが、第 4 回で訂正した」 | 訂正後の記述だけを書く。旧記述は残さない |
| 「前回の読み違いを補正」「追加調査で判明」 | 判明した内容だけを書く |
| 「2026-09-01 ユーザー確認済み」「オーナー決定」 | 決まった内容だけを書く |
| 経緯・試行錯誤・誰がいつ何と言ったか | 書かない。それは `log/` の仕事 |

**間違いが見つかったら、本文を直接書き直してください。** 注記を足して両論併記にしないこと。
何が正しいかが読み手に分からなくなり、読む時間もトークンも二重にかかります。
**訂正の履歴を残したいなら `log/` に残ります。** 統合版は履歴の置き場所ではありません。

根拠は **`raw/` のファイル名だけ**添えます（例: 「…CloudFront は 12 件（`raw-cf.json`）」）。
`log/` の節番号を出典にしないでください。読み手をログへ連れ戻すことになります。

**`raw/` に無いもの（口頭で聞いた背景、複数の API を突き合わせて初めて言えること）は、
出典を書かずに言い切ってください。** 報告書は言い切るものです。裏を取りたい人は `raw/` を引きます。

⚠️ ただし **「事実と推測を分ける」は統合版でも守ります**（大原則 2）。
出典を落とすことと、推測を事実のように書くことは別です。

### 置き場所の早見表

| 置くもの | 置き場所 |
| --- | --- |
| その回の調査ログ（Markdown） | `out/.survey/log/NN-<topic>.md`（英語名。例 `01-baseline.md`・`03-ec2-app1.md`）。Write ツールで保存する |
| `aws` / `ec2` の生の出力 | `out/.survey/raw/`（例: `> out/.survey/raw/raw-sg.json`） |
| 分かったことの結論 | `report/構成報告.md`（本文を書き直す。上記） |
| 人が調べること・中を見れば分かること | `report/ユーザー確認事項.md`（下記） |
| ユーザーから聞いた、対象そのものの前提 | `report/前提.md`（下記） |
| 次も効く調べ方・読み違いの類型 | `out/.survey/notes.md`（下記） |
| 報告の検証記録 | `out/.survey/log/NN-verify.md`（`method/05`） |

### 台帳

台帳は `out/.survey/state.md` の 1 本です。見出しは次の 5 つで固定し、回を重ねても増やしません。

```
# 台帳
## 作業        いま何の仕事をしているか。仕事が複数なら 1 行ずつ、状態（報告済み・検証済み・ユーザーの回答待ち）を添えて
## 現在地      どこまで終わり、何が途中か
## 次にやること 番号つき。ユーザーが何も言わないときの既定
## 未解決      分からないままのこと・読めなかったもの
## 決定事項    このアカウントを扱ううえで決めたこと（アカウント ID・ARN・IP の扱いなど）
```

薄く保ってください。その回に何をしたかは `log/` に、分かったことは `report/` に書き、台帳には「いまどこか」だけを残します。
生データの索引は要りません。`raw/` のファイル名がそのまま索引です。

### `out/.survey/notes.md` に、次も効くことを書く

調べていると「**次からはこう調べればいい**」と分かる瞬間があります。
それを会話の中に置いたままにすると、セッションが終わった時点で消えます。

| 書くもの | 例 |
| --- | --- |
| 効いた調べ方 | どの API を叩くと、何が一度に分かるか |
| 読み違いの類型 | 何を信じると間違えるか（名前・作成日時・一覧の欠落など） |
| 遠回りだったこと | 次はやらなくてよいこと |

例はこのアカウントのもので構いませんが、**教訓は別のアカウントでも通じる形で書いてください。**
台帳（現在地）とも、前提（対象そのものの前提）とも別のファイルです。混ぜないこと。

**ここに書いてあることと違う挙動を見つけたら、「違った」と書いてください。**
この文書（`method/`）は書き換えられませんが、`out/.survey/notes.md` には書けます。

### ユーザーには、仕組みではなく中身で話す

ユーザーは `method/` を読んでいません。台帳・統合版・検証・`raw/`・`log/` といった言葉は、
この作業環境の内側の仕組みで、ユーザーの言葉ではありません。返答には出さないでください。
言うのは「何を調べるか」「何が分かったか」「ユーザーにしてほしいことは何か」の 3 つです。

| 内側の言い方（出さない） | ユーザーへの言い方 |
| --- | --- |
| 予定に無いので、次の回に回します | 分かりました。いま調べます（…調べて答える） |
| 統合版の検証が済んでいません | 前回まとめた報告を、いちど見直してから続けます |
| 台帳に積んであります | あとで調べる予定に入れてあります |
| `raw/` を引き直しました | 前に取った結果から分かりました |
| 資格情報が切れました。再発行を依頼します | 作業環境の外で `cd … && aws-survey credentials` を実行してください（環境は開いたままで構いません） |

「仕組み上できません」「予定に無いのでできません」で返答を終えないでください。できない理由を言う場面でも、
代わりにユーザーが何をすればよいかを 1 本のコマンドか「はい」の一言まで落として添えます。

### ユーザーに聞くのは、あなたの仕事です

対象システムについて、あらかじめ用意された資料はありません。
リポジトリも設計書も見えず、`out/` にあるのは、これまでのセッションが自分で書いたものだけです。

だから、聞かないと分からないことは、その場でユーザーに聞いてください。
この作業環境ではユーザーと対話できます。推測で埋めないでください。
ただし、**AWS の中で調べれば決まることは、聞く前に調べます。** 聞くのは AWS の外のこと（利用者・運用の事情・他のサービス・名前の意味・意図）です。
例外は、最初の指示に「ユーザーが応答できない」とある回だけです。その回は聞かずに `report/ユーザー確認事項.md` に積み、
報告まで書き切ります（`00_開始と再開.md`）。推測で埋めないのは同じです。

聞くことの例

- 「このシステムは何をするものですか」「利用者は誰ですか」
- 「このリソースは意図してこうなっていますか」「これはもう使っていませんか」
- 「`◯◯` という名前は何を指しますか」「この環境は誰が使っていますか」
- 「`prod` / `stg` / `dev` はそれぞれどれですか。AWS 上に無いものはありますか」

| 状況 | どうする |
| --- | --- |
| その答えが無いと、次の調査手順が決まらない | すぐ聞く。推測で進めない |
| その答えで、いま書いている記述が変わる | すぐ聞く |
| 後でまとめて答えてもらえばよい | `report/ユーザー確認事項.md` に積む（下記） |
| ユーザーが席にいない・返事が来ない | `report/ユーザー確認事項.md` に積んで、調査は先へ進める |

聞いた答えは、必ず `report/前提.md` に書いてください。
会話の中だけに残すと、セッションが終わった時点で消えます。
この環境は使い捨てで、`out/` だけが記憶です。

### `report/前提.md` を育てる

これは「対象システムについて、ユーザーから聞いて分かったこと」を積み上げていくファイルです。
初回は見出しだけから始まります。セッションごとに書き足してください。ユーザーが自分で直してもよいファイルです。

| 書くこと | 例 |
| --- | --- |
| 何のシステムか・利用者は誰か | ユーザーに聞く |
| アプリケーションの構成要素 | ユーザーに聞く |
| 世代と系統（旧環境・現行・進行中のものが並行していないか） | ユーザーに聞く。これを外すと読み違えます |
| 名前と実態が一致しないもの | ユーザーに聞く。名前から用途を推測しないため |
| `prod` / `stg` / `dev` の実体（AWS 上に無い環境があるか） | ユーザーに聞く |
| 固有名詞・略語の意味 | ユーザーに聞く |

AWS の構成そのものは、ここに書かないでください。 それは調査結果であって、統合版に書きます。
ここに書くのは、AWS を見ても分からない、対象そのものの前提です。

出どころを必ず書いてください（「ユーザーに確認済み」／「未確認・推測」）。
後から読む人が、どこまで裏が取れているかを判断できる必要があります。

前のセッションが書いたことでも、実態と食い違って見えたら鵜呑みにしないでください。
ユーザーに聞いて、直してください。

### その場で答えが出ないものは、調査結果に混ぜない

聞けなかった問い・すぐには答えが出ない問いは、`report/ユーザー確認事項.md` に分けて集めてください。
ユーザーはこのファイルの回答欄に書いて返します。書き方は `04`「`report/ユーザー確認事項.md`」にあります。要点は次の 3 つです。

- 1 つの問いは、質問 1 文（見出し）・背景 2〜3 文・回答欄の 3 つで書く。問いの吟味に使った理由づけを、そのまま並べない
- それ単体を読むだけで意図が分かるようにする。調査結果を開かないと意味が通らない書き方をしない
- 回答欄に書かれていたら、それをユーザーの答えとして扱う。`report/前提.md` に移し、統合版の本文を書き直し、その問いを消す

基礎調査では、指摘リストやリスク一覧の体裁で書きません（`04`）。
これは「構成を読み解くために事実が足りない箇所」の一覧です。

---

## コマンドの作法

| 決まり | 理由 |
| --- | --- |
| `aws` は単独で実行する | パイプ・`&&`・`bash -c` はこの環境では実行できません（`> out/…/名前.json` の保存だけは通ります） |
| EC2 の中は `ec2 <host> <動詞>` で | 登録済みのホストに限る。`aws` と同じ作法（単独で実行、保存は `> out/…/名前.txt`）。動詞と進め方は `06_EC2の中を調べる.md` |
| Lambda のコードは `code/` を読む | 取り出してある関数に限る。コードの URL はこの環境では取得できない。読み方は `07_Lambdaのコードを読む.md` |
| 絞り込みは `--query` と `--output` | 上記の制約下での標準手段 |
| `--max-items` を必ず付ける | API レート制限と、本番の CloudTrail 汚染を防ぐため |
| 生データは `> out/.survey/raw/raw-名前.json` で保存する | 出力が文脈を経由するとトークンを浪費するため |
| レポート（Markdown）の保存は Write ツール | |
| リージョンは指定しなくてよい | 既定のリージョンが設定済みです。他リージョンを見る必要が出たら `--region` を明示し、理由を添える |

### この環境で通らない書き方と、その回避

`aws` を含むコマンドは、`;` やパイプ、リダイレクトと組み合わせると実行できません。
`> out/.survey/raw/名前.json` への保存だけが例外です（相対パスのみ。絶対パスは通りません）。

| やりたいこと | 通らない書き方 | 通る書き方 |
| --- | --- | --- |
| 長いコマンドを読みやすくする | 改行、`\` による行継続 | 1 行で書く。引数が多いときは `--cli-input-json file://out/.survey/raw/名前.json` でファイルに渡す |
| 文字列 `aws` を含むパターンで絞る | `grep '^/aws/lambda/' … \| head`・`sed -n '/aws/p' …` | `grep` / `rg` / `cat` / `head` / `wc` / `ls` をファイルに対して**単独で**実行するなら、`aws` や `boto3` を書いてよい（`grep -rn boto3 <ディレクトリ>`）。それ以外（`sed` や `jq` のフィルタ）では、`sed -n 's#^/.ws/lambda/##p'`・`test("^/.ws-service-role/")` のように、**ワイルドカードで `aws` の字面を避ける** |
| 同じ API を対象を変えて何度も叩く | `for` ループ | 1 つずつ実行する。**独立したコマンドは 1 回の応答でまとめて投げると速い** |
| 多数の対象を 1 回で問い合わせる | `for` ループ | ⭐ **クエリを JSON に書いてファイルで渡す**（例: `cloudwatch get-metric-data --metric-data-queries file://out/.survey/raw/queries.json`）。ループが使えない制約の、最も効く回避策 |
| 出力をファイルに落とす | 絶対パス | 相対パスの `> out/.survey/raw/名前.json`。位置引数で出力先を取る API（`cloudfront get-function` など）にも同じ形で渡せる |

### `--query`（JMESPath）の癖

- バックティックは使えません。`||` と `|`（パイプ演算子）も通りません
- `keys()` は使えます（例: `keys(Environment.Variables)`）。
  **ただし対象が `null` だとエラーになる**ので、`[?Environment.Variables!=null]` で先に絞ってください
- `--filter` に渡す JSON は、エスケープすれば通ります

### ページングの癖

**`--max-items` が効かない API があります。** `--max-results` や `--limit` を使うか、上限が API 側で決まっています。

`apprunner list-services` / `describe-service`（`--max-results`・上限 20）/ `wafv2 list-web-acls`（`--limit`）/
`timestream-write list-databases` / `synthetics describe-canaries` / `workmail list-*` / `chatbot describe-*` /
`codestar-notifications list-notification-rules`（`--max-results`）/ `budgets describe-*`（`--max-results`）/
`imagebuilder list-*`（`--max-results`）

`resourcegroupstaggingapi get-resources` は `--resources-per-page` と `--max-items` を併用できません。

この一覧が網羅である保証はありません。弾かれたら、そのコマンドの `help` で対応するオプションを確かめてください。

## 大きな出力の扱い

`aws` の出力をそのまま受け取ると、リソースが多いときにトークンを浪費し、
限られたセッション時間の中で調査が終わりません。次の 2 段階に分けてください。

```
aws ec2 describe-instances --max-items 100 > out/.survey/raw/raw-ec2.json
jq '[.Reservations[].Instances[] | {id:.InstanceId, type:.InstanceType, state:.State.Name}]' out/.survey/raw/raw-ec2.json
```

| 決まり | 内容 |
| --- | --- |
| 保存先 | いまの作業の `raw/` 配下。拡張子は `json` / `txt` / `csv` |
| 命名 | 生データは `raw-` で始める（レポートと区別するため） |
| 読み方 | `cat` で全部読まない。 `jq` で必要な部分だけ取り出す |
| 再利用 | 保存した JSON は何度でも別の観点で問い合わせ直せる。 API を叩き直さないこと |
| 過去の仕事の `raw/` | 読んでよい。むしろ先に読む。 同じ問いに API を叩き直さない |

最後の 2 点が重要です。API の再実行は、レート制限と本番の CloudTrail 汚染につながります。

## 効率のよい入口

全リージョン・全サービスを総当たりしないでください。次の 2 つで当たりを付けてから深掘りします。

```
aws ce get-cost-and-usage --time-period Start=<先月の1日>,End=<今月の1日> --granularity MONTHLY --metrics UnblendedCost --group-by Type=DIMENSION,Key=SERVICE --region us-east-1
```
→ 課金が発生しているサービス＝実際に動いているものが 1 回で分かります。
   Cost Explorer は `us-east-1` のみです。`Key=REGION` に変えればリージョンの当たりも付きます。

### Cost Explorer が使えないとき

このコマンドは、アカウント側で Cost Explorer が有効になっていないと通りません。**よくあることで、この環境の制約ではなく
アカウントの設定です。** 出たエラーで見分けてください。

| エラー | 意味 | すること |
| --- | --- | --- |
| `DataUnavailableException`（`Data is not available`） | Cost Explorer が未有効化か、有効化した直後でデータがまだ無い | 有効化を依頼する（下記）。有効化から反映まで 24 時間ほどかかる |
| `AccessDeniedException` | 有効化とは別の話。IAM ロールからの請求情報へのアクセスがアカウントで許可されていない、または組織のポリシーで塞がれている | 下記の文面で、有効化と一緒に「IAM アクセスの有効化」も依頼する |
| `ExpiredToken` など、`ce` 以外でも出るもの | Cost Explorer とは無関係 | 「セッションの型」の該当項目 |

どちらでも、**その場でユーザーに伝えてください。** 数リージョン・数サービスを総当たりして見つける作業が、
1 回の API で済むようになります。しかも今日頼んでおけば明日には使えるので、頼むのは早いほど得です。
ただし、待たずに調査は続けてください。代わりの入口は `resourcegroupstaggingapi get-resources`（タグ無しは出ない）と、
`ec2 describe-regions` で得たリージョンごとの `describe-*` です。

ユーザーへの伝え方は「仕組みではなく中身で話す」の型に沿います。エラー名やコマンドを読ませず、
してほしいことを 1 つに落とします。文面の例:

> このアカウントでは、サービスごとの利用料金（Cost Explorer）がまだ見られない設定になっています。
> 見られるようにしていただくと、実際に動いているサービスとリージョンが 1 回で分かり、調査が速く正確になります。
> AWS のコンソールで「請求とコスト管理」→「Cost Explorer」を開くだけで有効になり（無料）、翌日から使えます。
> 開いても見られない場合は、ルートユーザーで「アカウント」設定の「IAM ユーザーおよびロールによる請求情報へのアクセス」を
> 有効にしてください。いまの調査は待たずに続けますので、都合のよいときで構いません。

有効化された翌日以降のセッションで、冒頭のコマンドをもう一度試してください。使えるようになった日と、
それまで代わりに何で当たりを付けたかは `out/.survey/notes.md` に残します（「読めなかったものも記録する」）。

```
aws resourcegroupstaggingapi get-resources --resources-per-page 100 > out/.survey/raw/raw-tagged.json
```
→ タグ付きリソースの一覧。タグの無いものは出ない点に注意してください。
   件数が多いのでファイルに落としてから `jq` で読んでください。
